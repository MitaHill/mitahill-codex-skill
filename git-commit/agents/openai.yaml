interface:
  display_name: "git-commit"
  short_description: "Draft a repo-compatible staged commit message"
  default_prompt: "Use $git-commit to draft a repository-compatible commit message from the current staged changes."
